<!DOCTYPE html>

<html lang="en">
  <head>
    <title>Tiny App -- HOMEPAGE</title>
  </head>
  <body>
    <% include partials/_header %>
    <h2>URLs</h2>
    <ul>
      <% for (var key in urls) { %>
        <li> shortURL:
          <%= key        %> longURL: %>
          <%= urls[key] %>
          <form method="POST" action="/urls/<%= key %>/delete">
            <button>DELETE &#x1f5d1 </button>
          </form>
          <form method="GET" action="/urls/<%= key %>">
            <button> edit </button>
          </form>
        </li>
      <% } %>
    </ul>
  </body>
</html>